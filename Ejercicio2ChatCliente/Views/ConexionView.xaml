<UserControl x:Class="Ejercicio2ChatCliente.Views.ConexionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Ejercicio2ChatCliente.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel HorizontalAlignment="Center">
        <TextBlock>Escribe la direccion ip del Servidor</TextBlock>
        <TextBox Margin="5" Text="{Binding IP}"></TextBox>
        <Button Margin="5" Command="{Binding ConectarCommand}">Conectar</Button>
    </StackPanel>
</UserControl>
